import React, { Component } from 'react';
import Marker from './Marker';

export default class Markers extends Component {
  // generateMarkers(){
  //   console.log('generating markers is a thing', this.state.poolList)
  //   for (let i = 0; i < this.props.poolList.length; i++){
  //     console.log(parseFloat(this.props.poolList[i].latitude));
  //     let marker = new window.google.maps.Marker({
  //       position: {
  //         lat: parseFloat(this.props.poolList[i].latitude),
  //         lng: parseFloat(this.props.poolList[i].longitude),
  //       },
  //       map: this.state.map,
  //       id: i
  //     })
  //   }
  // }

  render() {
    return (
      <div>
      </div>
    )
  }
}
